const tags = document.getElementsByTagName("li")
for(let i=0; i < tags.length ; i++){
    tags[i].style.color = "green"
}

const tag = document.getElementsByTagName("div")
for(let i=0; i < tag.length ; i++){
    tag[i].style.color = "purple"
}

const ta = document.getElementsByTagName("p")
for(let i=0; i < ta.length ; i++){
    ta[i].style.color = "blue"
}

const etiqueta = document.getElementsByTagName("h2")
for(let i=0; i < etiqueta.length ; i++){
    etiqueta[i].style.color = "brown"
}
